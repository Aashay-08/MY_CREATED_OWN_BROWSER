# MY_CREATED_OWN_BROWSER
For Windows Users
install_PyQt5.PyPI from google and paste(ctrl+v) it in Terminal.
For this-PyQt5.QtWebEngineWidgets...You will have to install-(pip install PyQtWebEngine) from google and paste it in Terminal.
